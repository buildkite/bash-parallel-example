name: "Bash Parallel Example"
description: "An example pipeline that runs a Bash script in parallel across multiple agents."
emoji: ":bash:"
languages:
  - "Shell"
steps:
  - command: "buildkite-agent pipeline upload"
    label: ":pipeline:"
